; drop — PHP limits for hosts running PHP-FPM/CGI (most shared hosts).
; Note: .user.ini changes can take up to 5 minutes to apply (user_ini.cache_ttl).
upload_max_filesize = 310M
post_max_size = 320M
max_execution_time = 600
max_input_time = 600
memory_limit = 256M
